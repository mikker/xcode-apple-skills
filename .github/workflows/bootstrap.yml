name: Bootstrap

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  bootstrap:
    runs-on: xcode-27
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: ./bootstrap.sh
      - run: test -s bundled/swiftui-specialist/SKILL.md
